---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart.fullname" . }}-config
  labels:
    release: {{ .Release.Name | quote }}
    chart: "{{ .Chart.Name }}"
data:
{{- range $path, $config := .Values.config }}
  {{ $path }}: |
{{ $config | indent 4 -}}
{{- end -}}